# NoName_project

Design-system foundation for a recognizable UI style:

- Brand gradients (primary + secondary)
- Light/dark neutral palette
- Adaptive depth (glass + elevation)
- Fixed visual consistency tokens
- Signature live-gradient header pattern

See `docs/design-system.md` and `examples/ui-showcase.html`.
